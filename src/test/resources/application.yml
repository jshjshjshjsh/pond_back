spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true

gemini:
  api:
    secret: api-test-code
jwt:
  secret: this-is-a-very-very-long-and-secure-secret-key-for-testing-secret-key!